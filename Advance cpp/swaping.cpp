#include <bits/stdc++.h>
using namespace std;

int main() {
  int s = 35;
  int p = 36;
  cout << "numbers before swaping are s = " << s << " and p = " << p << endl;
  swap(s, p);
  cout << "numbers after swaping are  s = " << s << " and p = " << p << endl;

  return 0;
}
